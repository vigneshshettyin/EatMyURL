{
  "name": "kafka-ch-mc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pubsub": "npx nodemon --env-file=.env ./redis/sub.js",
    "serve": "npx nodemon --env-file=.env ./index.js",
    "pubsub-kafka": "node --env-file=.env ./redis/sub.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "kafkajs": "^2.2.4",
    "morgan": "^1.10.0",
    "redis": "^4.6.14"
  }
}
